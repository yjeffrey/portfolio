define(['angular', './main'],
	function (angular, controllerModule, services) {
	
		controllerModule
		.controller('NavbarController', ['$scope', '$state', 'DemoService', 
		function($scope, $state, DemoService) {
			
		
		}]);
	
	});